#import

def gen_batch_function(data_folder, image_shape):

    def get_batches_fn(batch_size):
        """
        Create Batches of training data
        """
        image_path = glob(os.path.join(data_folder, 'image_2', '*.png'))
        
